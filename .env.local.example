# Local overrides — copy to .env.local and fill in.
# .env.local is gitignored; do not commit real values.

# Path to cookies (for restricted videos)
# COOKIES_FILE_PATH=/absolute/path/to/cookies.txt

# Whisper API (when WHISPER_MODE=api)
# WHISPER_API_KEY=sk-...

# Redis cache (when CACHE_MODE=redis)
# CACHE_REDIS_URL=redis://localhost:6379

# MCP HTTP auth (when exposing MCP remotely)
# MCP_AUTH_TOKEN=your-secret-token
